# 文化祭シフト調整アプリ

文化祭などの学校行事におけるスタッフの参加希望を集約し、シフトの状況を共有するためのWebアプリです。

## [ver.0.2.0] — 2026-09-06 14:15 JST
- **スマートフォン最適化**
  - シフト表をスマホ向けにカード型UIへ刷新し、横スクロール不要で閲覧できるように改善
  - 要確認の時間帯リストを折りたたみ（アコーディオン）表示に対応し、確認したいときだけ展開できるように改善
- **希望入力の動線改善**
  - シフト確認ページ上部に「参加希望を入力する」CTAバナーを設置し、入力画面へのアクセスをスムーズ化
- **名前選択と誤入力・上書き防止機構**
  - 初期表示を未選択にし、他人の名前が最初から選ばれる状態を防止
  - 入力開始前の本人確認ダイアログおよび、既存回答がある場合の上書き警告を表示
  - 選択した名前のローカル記憶（`localStorage`）とワンタップ解除ボタン（名前の変更）を追加
  - 他メンバー選択時の「閲覧専用モード（編集不可）」化
  - 即時自動保存から「希望を保存する」ボタンによる明示的な保存方式への変更
- **管理者（アドミン）権限保護**
  - メンバー管理・文化祭設定・全データ初期化を管理者パスコード（初期値: `admin123`）で保護
  - 設定画面からの管理者パスコード変更機能を追加
- **初期設定・データの整理**
  - メンバー初期値を空に設定
  - デフォルトの開催時間を 10:00〜15:00、シフト区分を 30分間隔 に調整
- **フッターのバージョン表記**
  - ページ最下部に `ver.0.2.0` を表示

## [ver.0.1.0] — 2026-09-04 17:27 JST
- 文化祭シフト調整アプリの初版を公開
- 参加メンバーごとの各時間帯の希望入力（3段階）
- シフト表でのリアルタイム集計・充足状況表示
- Firebase Firestore によるリアルタイム同期機能

## 概要

参加メンバーがそれぞれの時間帯について参加可否を入力し、その結果をリアルタイムに共有します。文化祭の開催時間、1シフトの長さ、時間帯ごとの必要人数などを設定できます。

ブラウザから利用でき、データの保存・共有にはFirebase Firestoreを使用しています。

## 主な機能

- **シフト表**
  - 時間帯ごとの担当メンバーを表示
  - 必要人数に対する充足状況を表示
- **希望入力**
  - メンバーごとに各時間帯の参加希望を入力
  - 「参加できる」「できれば参加」「参加できない」の3段階で登録
- **メンバー管理**
  - メンバーの追加・削除
- **文化祭設定**
  - 文化祭名の設定
  - 時間帯ごとの必要人数の設定
  - 開始時刻・終了時刻の設定
  - 1シフトの長さ（30分／1時間／1時間30分／2時間）の設定
  - 全データの削除
- **リアルタイム共有**
  - Firebase Firestoreを利用し、複数の端末からデータを共有

## 技術構成

- HTML
- CSS
- JavaScript
- Firebase
  - Firebase App
  - Cloud Firestore

外部サーバー側のアプリケーションを必要とせず、フロントエンドからFirebase Firestoreへ接続する構成です。

## 使い方

1. アプリをブラウザで開きます。
2. 「設定」から文化祭名、必要人数、開催時間、シフト時間を設定します。
3. 「メンバー」から参加メンバーを登録します。
4. 各メンバーが「希望入力」から時間帯ごとの参加希望を入力します。
5. 「シフト表」で各時間帯の状況を確認し、シフトを調整します。

## データについて

シフト情報や参加希望などのデータはFirebase Firestoreに保存されます。

本アプリは学校行事での利用を想定した初期版です。認証・権限管理などの高度なアクセス制御は、今後のバージョンでの検討事項です。

## ライセンス

現在、ライセンスは設定していません。
